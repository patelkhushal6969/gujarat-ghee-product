# Gujarat Ghee Product Landing Page

A premium, high-performance landing page for **Gujarat Ghee Product**, capturing the essence of "Earthy Luxury" and traditional Gujarati heritage. Built with **Next.js (App Router)**, **Tailwind CSS**, and **Framer Motion**.

## 🌟 Key Features

*   **Design Philosophy**: "Royal Gujarat" vibe using `Ghee Gold`, `Clay Red`, and `Deep Rich Brown` colors.
*   **Cinematic Animations**: Smooth, non-bouncy fade-ins using `framer-motion`.
*   **Mobile-First**: Fully responsive with a mobile-only **Sticky CTA** for easy WhatsApp conversion.
*   **SEO Optimized**: Semantic HTML, JSON-LD Schema (`Product`), and dynamic metadata.
*   **WhatsApp Integration**: Direct "Inquire & Buy" functionality with pre-filled messages.

## 🛠️ Tech Stack

*   **Framework**: [Next.js 15+](https://nextjs.org/) (App Router)
*   **Styling**: [Tailwind CSS](https://tailwindcss.com/)
*   **Animations**: [Framer Motion](https://www.framer.com/motion/)
*   **Icons**: [Lucide React](https://lucide.dev/)
*   **Fonts**: `Playfair Display` (Serif) & `Inter` (Sans-Serif) via `next/font`.

## 🚀 Getting Started

1.  **Clone the repository**:
    ```bash
    git clone https://github.com/your-username/gujarat-ghee-product.git
    cd gujarat-ghee-product
    ```

2.  **Install dependencies**:
    ```bash
    cmd /c npm install
    ```

3.  **Run the development server**:
    ```bash
    cmd /c npm run dev
    ```

4.  **Build for production**:
    ```bash
    cmd /c npm run build
    ```

## 📂 Project Structure

```
src/
├── app/
│   ├── globals.css      # Global styles & variables
│   ├── layout.tsx       # Root layout with Fonts & Metadata
│   └── page.tsx         # Main landing page assembly
├── components/
│   ├── Hero.tsx         # Hero section components
│   ├── Story.tsx        # 'Our Story' section
│   ├── Features.tsx     # Process & Benefits
│   ├── Footer.tsx       # Site Footer
│   └── StickyCTA.tsx    # Mobile-only sticky button
```

## 🎨 Customization

*   **Colors**: Defined in `tailwind.config.js` and `globals.css` (`ghee-gold`, `clay-red`, `deep-brown`, `cream`).
*   **Images**: Replace images in `public/images/`:
    *   `hero-jar.png`
    *   `gir-cows.png`
    *   `bilona-process.png`

## 📞 Support

For any inquiries, contact us via the website or WhatsApp.

---
Made with ❤️ in Gujarat.
